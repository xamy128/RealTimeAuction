extends layout
include productListMixin.pug

block content
    .container
    if IsSupplier
        .row.centered.new-prd-btn
            .col-xs-10
            .col-xs-2
                a(href="/ProductView").btn.btn-primary="Add New Product"
    .row.centered
        .col-xs-12
            +productList(productsList, title, "show")